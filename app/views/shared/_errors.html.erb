<% if target.errors.any? %>
  <h4><%= pluralize(target.errors.count, 'error') %> prevented this record from being saved:</h4>

  <ul>
    <% target.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

