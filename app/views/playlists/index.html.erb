<h1>All Playlists</h1>

<%= link_to "New Playlist", new_playlist_path %><br />
<hr />

<ul>
  <% @playlists.each do |playlist| %>
    <li><%= link_to playlist.name, playlist_path(playlist) %></li>
  <% end %>
</ul>
