<h1>All Artists</h1>

<ul>
  <% @artists.each do |artist| %>
    <li id="artist_<%= artist.id %>"><%= link_to artist.name, artist_path(artist) %></li>
  <% end %>
</ul>

<%= link_to 'New artist', new_artist_path %>
